package class_58_Assertion_In_TestNG;

import org.testng.annotations.Test;

public class HowToPass_TestCases_AssertionMethod_In_TestNG {
	@Test
	public void testcase1() {
		int a = 100;
		//Assert.assertTrue(true);// Assert is class here and will be only used in @Test in TestNG
		//Assert.assertTrue(false);// if I want my test case to be failed then I can use this
		//Assert.assertFalse(false);
		//Assert.assertFalse(a<99);
		//Assert.assertEquals(true, true);
		//Assert.assertEquals(true, false,"let's retry");
		//Assert.assertEquals("Chandan", "chandan","Sorry, two strings are not equals");
		//Assert.assertEquals('a', 'a');
		
	}

}
